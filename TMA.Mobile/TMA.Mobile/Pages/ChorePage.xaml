<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TMA.Mobile.Pages.ChorePage">
    <ContentPage.Content>
        
        <ScrollView>
            <ListView ItemsSource="{Binding Chores}" 
                      SelectedItem="{Binding SelectedChore}"
                      BackgroundColor="{StaticResource LightBlueBackGroundColor}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout>
                                    <Label Text="{Binding Name}" Style="{StaticResource ListLabel}" />
                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollView>

    </ContentPage.Content>
</ContentPage>